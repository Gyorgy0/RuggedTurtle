use crate::parsing::trim_whitespace;

pub fn parse_number_value(input: String) -> f64 {
    // Removes whitespaces
    let trimmed_input = trim_whitespace(input.as_str());
    let parenthesis_blocks: Vec<&str> = vec![];
    0_f64
}
